#include <stdio.h>
#include "ft_abs.h"

int	main(void)
{
	int	a;
	int	b;
	int	c;

	a = -5;
	b = 10;
	c = 0;
	printf("la valeur absolue de %d est : %d\n", a, ABS(a)));
	printf("la valeur absolue de %d est : %d\n", b, ABS(b)));
	printf("la valeur absolue de %d est : %d\n", c, ABS(c)));
	return (0);
}
